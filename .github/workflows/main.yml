YAML

name: My First pipeline

on: [push]

jobs:
 build:
  runs-on: ubuntu-latest

  steps:
   -  name: checkout code
      uses:actions/checkout@v3

   -  name: Echo message
      run: echo "pipeline running "
   
   
